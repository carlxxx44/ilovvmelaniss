/** @type {import('next').NextConfig} */
const nextConfig = {
  // Export static HTML with `next build` when using the App router
  output: 'export',
  // If you plan to publish to a project page (username.github.io/repo),
  // set the assetPrefix or basePath accordingly. Example:
  // basePath: '/ilovvmelaniss',
  // assetPrefix: '/ilovvmelaniss',
}

module.exports = nextConfig
